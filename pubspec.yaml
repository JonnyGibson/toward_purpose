name: toward_purpose
description: The Toward Purpose app
version: 1.0.0

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
 #   flutter_launcher_icons: "^0.9.2"
  # add any additional dependencies here

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/
  # add any additional Flutter-specific settings here
